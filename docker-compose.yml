services:
  rocky:
    build: .
    container_name: testrocky
    hostname: rocky8
    privileged: true
    cgroup: host
    volumes:
      - .:/opt/issabel-mineracao-src:ro
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    tmpfs:
      - /run
      - /run/lock
      - /tmp
    stop_signal: SIGRTMIN+3
